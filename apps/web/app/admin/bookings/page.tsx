'use client';
import { useEffect, useState } from 'react';
import { adminFetch } from '../../../lib/admin-api';
type Booking={id:string;bookingReference:string;status:string;client:{fullName:string;email:string};service:{name:string};slot:{startAt:string}};
const statuses=['CONFIRMED','COMPLETED','NO_SHOW','CANCELLED'];
export default function Bookings(){const [items,setItems]=useState<Booking[]>([]);const [error,setError]=useState('');const load=()=>adminFetch<Booking[]>('/admin/bookings').then(setItems).catch(e=>setError(e.message));useEffect(()=>{void load()},[]);return <><header className="admin-header"><div><div className="eyebrow">Reservations database</div><h1>Bookings</h1></div></header>{error&&<p className="form-error">{error}</p>}<section className="data-panel cut-corner"><table className="data-table"><thead><tr><th>Reference</th><th>Athlete</th><th>Service</th><th>Time</th><th>Status</th></tr></thead><tbody>{items.map(b=><tr key={b.id}><td className="accent">{b.bookingReference}</td><td>{b.client.fullName}<br/><small className="muted">{b.client.email}</small></td><td>{b.service.name}</td><td>{new Date(b.slot.startAt).toLocaleString('en-UG',{timeZone:'Africa/Kampala'})}</td><td><select value={b.status} onChange={async e=>{try{await adminFetch(`/admin/bookings/${b.id}/status`,{method:'PATCH',body:JSON.stringify({status:e.target.value})});void load()}catch(err){setError(err instanceof Error?err.message:'Update failed')}}}>{statuses.map(s=><option key={s}>{s}</option>)}</select></td></tr>)}</tbody></table></section></>}
