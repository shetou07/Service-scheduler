'use client'; import { useEffect,useState } from 'react'; import { adminFetch } from '../../../lib/admin-api';
type Client={id:string;fullName:string;email:string;phone:string;_count:{bookings:number}}; export default function Clients(){const [items,setItems]=useState<Client[]>([]);const [search,setSearch]=useState('');const [error,setError]=useState('');useEffect(()=>{const timer=setTimeout(()=>adminFetch<Client[]>(`/admin/clients?search=${encodeURIComponent(search)}`).then(setItems).catch(e=>setError(e.message)),200);return()=>clearTimeout(timer)},[search]);return <><header className="admin-header"><div><div className="eyebrow">Athlete roster</div><h1>Clients</h1></div></header><input className="field" placeholder="Search athlete, email, or phone" value={search} onChange={e=>setSearch(e.target.value)}/>{error&&<p className="form-error">{error}</p>}<section className="data-panel cut-corner"><table className="data-table"><thead><tr><th>Athlete</th><th>Contact</th><th>Bookings</th></tr></thead><tbody>{items.map(c=><tr key={c.id}><td>{c.fullName}</td><td>{c.email}<br/><small className="muted">{c.phone}</small></td><td>{c._count.bookings}</td></tr>)}</tbody></table></section></>}
