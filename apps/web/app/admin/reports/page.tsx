'use client'; import { useEffect,useState } from 'react'; import { adminFetch } from '../../../lib/admin-api'; type Report={bookings:{total:number;confirmed:number;cancelled:number;completed:number};utilization:number;capacity:{total:number;reserved:number}};export default function Reports(){const [report,setReport]=useState<Report|null>(null);const [error,setError]=useState('');useEffect(()=>{adminFetch<Report>('/admin/reports').then(setReport).catch(e=>setError(e.message))},[]);return <><header className="admin-header"><div><div className="eyebrow">Demand and utilisation</div><h1>Reports</h1></div></header>{error&&<p className="form-error">{error}</p>}{report&&<div className="metric-grid"><article className="metric-card cut-corner"><span>Total bookings</span><strong>{report.bookings.total}</strong></article><article className="metric-card cut-corner"><span>Confirmed</span><strong>{report.bookings.confirmed}</strong></article><article className="metric-card cut-corner"><span>Cancelled</span><strong>{report.bookings.cancelled}</strong></article><article className="metric-card cut-corner"><span>Utilisation</span><strong>{report.utilization}%</strong></article></div>}</>}
