import { PrismaClient, ServiceStatus } from '@prisma/client'; import bcrypt from 'bcrypt';
const prisma = new PrismaClient();
async function main() { const hash = await bcrypt.hash(process.env.SEED_ADMIN_PASSWORD || 'ChangeMe123!', 12); await prisma.admin.upsert({where:{email:process.env.SEED_ADMIN_EMAIL || 'admin@example.com'},update:{},create:{name:'Coach Rickie',email:process.env.SEED_ADMIN_EMAIL || 'admin@example.com',passwordHash:hash}}); for (const s of [{name:'Personal Training',description:'One-on-one coaching.',durationMinutes:60,priceMinor:150000},{name:'Public Training',description:'High-energy group workouts.',durationMinutes:45,priceMinor:0},{name:'Smash Room',description:'Stress-release session.',durationMinutes:30,priceMinor:80000},{name:'Recovery',description:'Mobility and recovery.',durationMinutes:30,priceMinor:60000}]) await prisma.service.upsert({where:{name:s.name},update:{},create:{...s,status:ServiceStatus.ACTIVE}}); }
main().finally(()=>prisma.$disconnect());
